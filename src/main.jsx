import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

//react hara un hook con un solo elemento html y renderizara con contenido
ReactDOM.createRoot(document.getElementById('root')).render(<App />);